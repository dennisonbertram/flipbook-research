# Experiment Logs

This folder is for measured runs, not plans. Keep raw outputs small, append-only where possible, and link larger artifacts from `outputs/`.

## Folders

- `track-a/` stores Track A model-layer benchmark logs.
