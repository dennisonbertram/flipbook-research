# Track C Autoresearch Status

Updated UTC: `2026-04-26T14:12:08+00:00`

## Active Sessions

- `agent-browser-chrome`
- `clawtest`
- `open-agents-web`
- `pr-1419-checks`
- `pr-1528-checks`
- `pr-1529-checks`
- `pr-1530-checks`
- `pr-1530-checks-2`
- `pr-1531-checks`
- `pr-1531-final-watch`
- `pr-1531-watch`
- `pr-1532-watch`
- `pr1337-gh-checks`
- `pr1419-checks-monitor`
- `pr1467-monitor`
- `pr1535-checks`
- `pr1535-wait`
- `pr1554-checks`
- `pubv2`
- `pubv3`
- `pubv31`
- `sexual-wellness-dev`
- `track-c-autoresearch`
- `track-c-c121-v13-glacier-indrecomp-blend-noscoord-truthrem075-tpow1-seed14-s12000`
- `track-c-c121-v13-glacier-indrecomp-blend-scoordmid0-truthrem075-tpow1-seed14-s12000`
- `track-c-c121-v14-microchip-indrecomp-talways-noscoord-truthrem075-tpow1-seed15-s12000`
- `track-c-c121-v14-microchip-indrecomp-talways-scoordfull-truthrem075-tpow1-seed15-s12000`
- `track-c-c121-v15-mycology-indrecomp-talways-noscoord-truthrem075-tpow025-seed16-s12000`
- `track-c-c121-v15-mycology-indrecomp-talways-scoordmid025-truthrem075-tpow025-seed16-s12000`
- `track-c-github-sync`
- `track-c-output-browser-8765`
- `track-v-output-browser-8766`
- `workflow-api`
- `workflow-frontend-dev`
- `workflow-worker`

## Queue

- `c121-v13-glacier-indrecomp-blend-noscoord-truthrem075-tpow1-seed14-s12000`: pending/running - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 14, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target glacier-field-guide.
- `c121-v13-glacier-indrecomp-blend-scoordmid0-truthrem075-tpow1-seed14-s12000`: pending/running - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 14, source-coord features midscale 0, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target glacier-field-guide.
- `c121-v14-microchip-indrecomp-talways-noscoord-truthrem075-tpow1-seed15-s12000`: pending/running - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 15, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target microchip-teardown.
- `c121-v14-microchip-indrecomp-talways-scoordfull-truthrem075-tpow1-seed15-s12000`: pending/running - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 15, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target microchip-teardown.
- `c121-v15-mycology-indrecomp-talways-noscoord-truthrem075-tpow025-seed16-s12000`: pending/running - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 16, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target mycology-field-guide.
- `c121-v15-mycology-indrecomp-talways-scoordmid025-truthrem075-tpow025-seed16-s12000`: pending/running - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 16, source-coord features midscale 0.25, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target mycology-field-guide.
- `c120-v11-naturalist-indrecomp-talways-noscoord-truthrem075-tpow025-seed13-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 13, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c120-v11-naturalist-indrecomp-talways-scoordmid025-truthrem075-tpow025-seed13-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 13, source-coord features midscale 0.25, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c120-v10-orbit-indrecomp-blend-noscoord-truthrem075-tpow1-seed12-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 12, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c120-v08-transit-indrecomp-blend-scoordmid0-truthrem075-tpow1-seed6-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 6, source-coord features midscale 0, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target transit-illustration.
- `c120-v09-reef-indrecomp-talways-scoordfull-truthrem075-tpow1-seed10-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 10, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c120-v07-timeline-indrecomp-blend-noscoord-truthrem075-tpow1-seed9-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 9, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c120-v12-deep-sea-indrecomp-tgated-noscoord-truthrem075-tpow1-seed11-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 11, latent cross neighborhood r1px source, target-state canvas gated init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c119-v11-naturalist-indrecomp-talways-noscoord-truthrem075-tpow025-seed12-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 12, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c119-v11-naturalist-indrecomp-talways-scoordmid025-truthrem075-tpow025-seed12-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 12, source-coord features midscale 0.25, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c119-v10-orbit-indrecomp-blend-noscoord-truthrem075-tpow1-seed11-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 11, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c119-v07-timeline-indrecomp-blend-scoordmid0-truthrem075-tpow1-seed9-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 9, source-coord features midscale 0, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c119-v12-deep-sea-indrecomp-tgated-scoordmid0-truthrem075-tpow1-seed11-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 11, source-coord features midscale 0, latent cross neighborhood r1px source, target-state canvas gated init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c119-v08-transit-indrecomp-blend-scoordmid0-truthrem075-tpow1-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 5, source-coord features midscale 0, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target transit-illustration.
- `c119-v09-reef-indrecomp-talways-noscoord-truthrem075-tpow1-seed9-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 9, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c119-v09-reef-indrecomp-talways-scoordfull-truthrem075-tpow1-seed9-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 9, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c118-v11-naturalist-indrecomp-talways-scoordmid0-truthrem075-tpow025-seed11-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 11, source-coord features midscale 0, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c118-v11-naturalist-indrecomp-talways-scoordmid025-truthrem075-tpow025-seed11-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 11, source-coord features midscale 0.25, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c118-v10-orbit-indrecomp-blend-scoordmid0-truthrem075-tpow1-seed10-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 10, source-coord features midscale 0, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c118-v07-timeline-indrecomp-blend-scoordmid0-truthrem075-tpow1-seed8-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 8, source-coord features midscale 0, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c118-v12-deep-sea-indrecomp-tgated-scoordmid0-truthrem075-tpow1-seed10-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 10, source-coord features midscale 0, latent cross neighborhood r1px source, target-state canvas gated init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c118-v09-reef-indrecomp-talways-scoordmid0-truthrem075-tpow1-seed8-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 8, source-coord features midscale 0, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c118-v08-transit-indrecomp-blend-scoordmid0-truthrem075-tpow1-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 4, source-coord features midscale 0, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target transit-illustration.
- `c117-v11-naturalist-indrecomp-blend-noscoord-truthrem075-tpow025-seed11-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 11, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c117-v11-naturalist-indrecomp-talways-noscoord-truthrem075-tpow025-seed11-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 11, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c117-v10-orbit-indrecomp-blend-noscoord-truthrem075-tpow1-seed10-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 10, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c117-v07-timeline-indrecomp-blend-noscoord-truthrem075-tpow1-seed8-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 8, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c117-v12-deep-sea-indrecomp-talways-noscoord-truthrem075-tpow1-seed10-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 10, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c117-v12-deep-sea-indrecomp-tgated-noscoord-truthrem075-tpow1-seed10-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 10, latent cross neighborhood r1px source, target-state canvas gated init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c117-v09-reef-indrecomp-talways-noscoord-truthrem075-tpow1-seed8-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 8, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c117-v08-transit-indrecomp-blend-noscoord-truthrem075-tpow1-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 4, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target transit-illustration.
- `c116-v11-naturalist-indrecomp-blend-noscoord-truthrem075-tpow025-seed10-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 10, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c116-v11-naturalist-indrecomp-talways-noscoord-truthrem075-tpow025-seed10-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 10, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c116-v10-orbit-indrecomp-blend-noscoord-truthrem075-tpow1-seed9-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 9, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c116-v07-timeline-indrecomp-blend-noscoord-truthrem075-tpow1-seed7-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 7, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c116-v12-deep-sea-indrecomp-talways-noscoord-truthrem075-tpow1-seed9-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 9, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c116-v12-deep-sea-indrecomp-tgated-noscoord-truthrem075-tpow1-seed9-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 9, latent cross neighborhood r1px source, target-state canvas gated init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c116-v09-reef-indrecomp-talways-noscoord-truthrem075-tpow1-seed7-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 7, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c116-v08-transit-indrecomp-blend-noscoord-truthrem075-tpow1-seed3-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 3, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target transit-illustration.
- `c115-v08-transit-indrecomp-blend-truthrem075-tpow1-seed3-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 3, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target transit-illustration.
- `c115-v08-transit-indrecomp-talways-truthrem075-tpow1-seed3-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 3, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target transit-illustration.
- `c115-v09-reef-indrecomp-blend-truthrem075-tpow1-seed7-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 7, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c115-v09-reef-indrecomp-talways-truthrem075-tpow1-seed7-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 7, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c114-v11-naturalist-indrecomp-blend-truthrem075-tpow025-seed10-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 10, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c114-v11-naturalist-indrecomp-talways-truthrem075-tpow025-seed10-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 10, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c114-v10-orbit-indrecomp-blend-truthrem075-tpow1-seed9-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 9, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c114-v10-orbit-indrecomp-talways-truthrem075-tpow1-seed9-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 9, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c114-v12-deep-sea-indrecomp-talways-truthrem075-tpow1-seed9-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 9, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c114-v12-deep-sea-indrecomp-tgated-truthrem075-tpow1-seed9-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 9, source-coord features, latent cross neighborhood r1px source, target-state canvas gated init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c114-v07-timeline-indrecomp-blend-truthrem075-tpow1-seed7-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 7, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c114-v07-timeline-indrecomp-talways-truthrem075-tpow1-seed7-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 7, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c114-v09-reef-indrecomp-talways-truthrem075-tpow1-seed7-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 7, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-poster.
- `c114-v08-transit-indrecomp-talways-truthrem075-tpow1-seed3-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 3, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target transit-map.
- `c113-v11-naturalist-indrecomp-blend-truthrem075-tpow025-seed9-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 9, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c113-v11-naturalist-indrecomp-tgated-truthrem075-tpow025-seed9-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 9, source-coord features, latent cross neighborhood r1px source, target-state canvas gated init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c113-v11-naturalist-indrecomp-talways-truthrem075-tpow025-seed9-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 9, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c113-v10-orbit-indrecomp-blend-truthrem075-tpow1-seed8-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 8, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c113-v10-orbit-indrecomp-tgated-truthrem075-tpow1-seed8-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 8, source-coord features, latent cross neighborhood r1px source, target-state canvas gated init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c113-v10-orbit-indrecomp-talways-truthrem075-tpow1-seed8-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 8, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c113-v07-timeline-indrecomp-tgated-truthrem075-tpow1-seed6-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 6, source-coord features, latent cross neighborhood r1px source, target-state canvas gated init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c113-v07-timeline-indrecomp-talways-truthrem075-tpow1-seed6-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 6, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c113-v12-deep-sea-indrecomp-tgated-truthrem075-tpow1-seed8-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 8, source-coord features, latent cross neighborhood r1px source, target-state canvas gated init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c113-v12-deep-sea-indrecomp-talways-truthrem075-tpow1-seed8-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 8, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c112-v11-naturalist-indrecomp-blend-truthrem075-tpow025-control-seed8-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 8, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c112-v11-naturalist-indrecomp-talways-truthrem075-tpow025-control-seed8-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 8, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c112-v11-naturalist-indrecomp-statesplit-truthrem075-tpow025-control-seed8-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 8, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c112-v11-naturalist-indrecomp-statesplit-truthrem075-tpow025-directsrc018-seed8-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, source-remnant direct 0.18 region source-only, seed 8, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c112-v07-timeline-indrecomp-blend-truthrem075-tpow1-control-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c112-v07-timeline-indrecomp-talways-truthrem075-tpow1-control-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c112-v10-orbit-indrecomp-blend-truthrem075-tpow1-control-seed7-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 7, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c112-v10-orbit-indrecomp-talways-truthrem075-tpow1-control-seed7-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 7, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c112-v12-deep-sea-indrecomp-blend-truthrem075-tpow1-control-seed7-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 7, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c112-v12-deep-sea-indrecomp-talways-truthrem075-tpow1-control-seed7-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 7, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c111-v11-naturalist-indrecomp-blend-truthrem075-tpow025-control-seed7-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 7, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c111-v11-naturalist-indrecomp-blend-truthrem075-tpow025-directsrc018-seed7-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, source-remnant direct 0.18 region source-only, seed 7, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c111-v11-naturalist-indrecomp-talways-truthrem075-tpow025-control-seed7-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 7, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c111-v11-naturalist-indrecomp-talways-truthrem075-tpow025-directsrc018-seed7-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, source-remnant direct 0.18 region source-only, seed 7, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c111-v11-naturalist-indrecomp-statesplit-truthrem075-tpow025-control-seed7-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 7, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c111-v11-naturalist-indrecomp-statesplit-truthrem075-tpow025-directsrc018-seed7-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, source-remnant direct 0.18 region source-only, seed 7, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c111-v08-transit-indrecomp-blend-truthrem075-tpow1-control-seed2-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 2, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target transit-illustration.
- `c111-v08-transit-indrecomp-talways-truthrem075-tpow1-control-seed2-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 2, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target transit-illustration.
- `c111-v09-reef-indrecomp-blend-truthrem075-tpow1-control-seed6-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 6, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c111-v09-reef-indrecomp-talways-truthrem075-tpow1-control-seed6-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 6, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c110-v11-naturalist-indrecomp-truthrem075-tpow05-control-seed6-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.5, seed 6, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c110-v11-naturalist-indrecomp-truthrem075-tpow05-directsrc018-seed6-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.5, source-remnant direct 0.18 region source-only, seed 6, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c110-v11-naturalist-indrecomp-truthrem075-tpow025-control-seed6-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 6, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c110-v11-naturalist-indrecomp-truthrem075-tpow025-directsrc018-seed6-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, source-remnant direct 0.18 region source-only, seed 6, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c110-v10-orbit-indrecomp-truthrem075-tpow1-control-seed6-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 6, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c110-v10-orbit-indrecomp-truthrem075-tpow1-directsrc025-seed6-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, source-remnant direct 0.25 region source-only, seed 6, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c110-v09-reef-indrecomp-truthrem075-tpow1-control-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c110-v09-reef-indrecomp-truthrem075-tpow1-directsrc018-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, source-remnant direct 0.18 region source-only, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c110-v08-transit-indrecomp-truthrem075-tpow1-control-seed1-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 1, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target transit-illustration.
- `c110-v08-transit-indrecomp-truthrem075-tpow1-directsrc018-seed1-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, source-remnant direct 0.18 region source-only, seed 1, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target transit-illustration.
- `c109-v11-naturalist-indrecomp-truthrem075-tpow025-directsrc015-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, source-remnant direct 0.15 region source-only, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c109-v11-naturalist-indrecomp-truthrem075-tpow025-directsrc018-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, source-remnant direct 0.18 region source-only, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c109-v11-naturalist-indrecomp-truthrem075-tpow05-directsrc015-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.5, source-remnant direct 0.15 region source-only, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c109-v11-naturalist-indrecomp-truthrem075-tpow05-directsrc018-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.5, source-remnant direct 0.18 region source-only, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c109-v10-orbit-indrecomp-truthrem075-tpow1-directsrc025-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, source-remnant direct 0.25 region source-only, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c109-v12-deep-sea-indrecomp-truthrem075-tpow1-directsrc015-seed6-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, source-remnant direct 0.15 region source-only, seed 6, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c108-v07-timeline-indrecomp-truthrem075-tpow1-directsrc025-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, source-remnant direct 0.25 region source-only, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c108-v10-orbit-indrecomp-truthrem075-tpow1-directsrc025-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, source-remnant direct 0.25 region source-only, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c108-v09-reef-indrecomp-truthrem075-tpow1-directsrc025-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, source-remnant direct 0.25 region source-only, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c108-v12-deep-sea-indrecomp-truthrem075-tpow1-directsrc025-seed6-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, source-remnant direct 0.25 region source-only, seed 6, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c108-v11-naturalist-indrecomp-truthrem075-tpow05-directsrc025-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.5, source-remnant direct 0.25 region source-only, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c108-v11-naturalist-indrecomp-truthrem075-tpow025-directsrc025-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, source-remnant direct 0.25 region source-only, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c107-v07-timeline-indrecomp-truthrem075-tpow1-directsrc050-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, source-remnant direct 0.5 region source-only, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c107-v10-orbit-indrecomp-truthrem075-tpow1-directsrc050-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, source-remnant direct 0.5 region source-only, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c107-v09-reef-indrecomp-truthrem075-tpow1-directsrc050-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, source-remnant direct 0.5 region source-only, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c107-v12-deep-sea-indrecomp-truthrem075-tpow1-directsrc050-seed6-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, source-remnant direct 0.5 region source-only, seed 6, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c107-v11-naturalist-indrecomp-truthrem075-tpow05-directsrc050-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.5, source-remnant direct 0.5 region source-only, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c107-v11-naturalist-indrecomp-truthrem075-tpow025-directsrc050-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, source-remnant direct 0.5 region source-only, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c106-v07-timeline-indrecomp-truthrem075-tpow1-direct035-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, source-remnant direct 0.35, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c106-v10-orbit-indrecomp-truthrem075-tpow1-direct035-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, source-remnant direct 0.35, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c106-v09-reef-indrecomp-truthrem075-tpow1-direct035-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, source-remnant direct 0.35, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c106-v12-deep-sea-indrecomp-truthrem075-tpow1-direct035-seed6-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, source-remnant direct 0.35, seed 6, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c106-v11-naturalist-indrecomp-truthrem075-tpow05-direct035-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.5, source-remnant direct 0.35, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c106-v11-naturalist-indrecomp-truthrem075-tpow025-direct035-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, source-remnant direct 0.35, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c105-v07-timeline-indrecomp-truthrem075-tpow1-srcsample06-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1 srcsample 0.06, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c105-v10-orbit-indrecomp-truthrem075-tpow1-srcsample06-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1 srcsample 0.06, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c105-v09-reef-indrecomp-truthrem075-tpow1-srcsample06-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1 srcsample 0.06, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c105-v12-deep-sea-indrecomp-truthrem075-tpow1-srcsample06-seed6-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1 srcsample 0.06, seed 6, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c105-v11-naturalist-indrecomp-truthrem075-tpow05-srcsample06-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.5 srcsample 0.06, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c105-v11-naturalist-indrecomp-truthrem075-tpow025-srcsample06-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25 srcsample 0.06, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c104-v07-timeline-indrecomp-truthrem075-tpow1-srcsample18-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1 srcsample 0.18, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c104-v10-orbit-indrecomp-truthrem075-tpow1-srcsample18-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1 srcsample 0.18, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c104-v09-reef-indrecomp-truthrem075-tpow1-srcsample18-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1 srcsample 0.18, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c104-v12-deep-sea-indrecomp-truthrem075-tpow1-srcsample18-seed6-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1 srcsample 0.18, seed 6, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c104-v11-naturalist-indrecomp-truthrem075-tpow05-srcsample18-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.5 srcsample 0.18, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c104-v11-naturalist-indrecomp-truthrem075-tpow025-srcsample18-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25 srcsample 0.18, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c103-v07-timeline-indrecomp-truthrem075-tpow05-seed3-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.5, seed 3, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c103-v10-orbit-indrecomp-truthrem075-tpow05-seed3-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.5, seed 3, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c103-v09-reef-indrecomp-truthrem075-tpow05-seed3-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.5, seed 3, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c103-v12-deep-sea-indrecomp-truthrem075-tpow05-seed5-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.5, seed 5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c103-v11-naturalist-indrecomp-truthrem075-tpow05-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c103-v11-naturalist-indrecomp-truthrem075-tpow025-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.25, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c103-v11-naturalist-indrecomp-truthrem100-tpow05-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 1 ref truth timepow 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c102-v07-timeline-indrecomp-truthrem075-tpow1-seed2-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 2, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c102-v10-orbit-indrecomp-truthrem075-tpow1-seed2-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 2, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c102-v09-reef-indrecomp-truthrem075-tpow1-seed2-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 2, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c102-v11-naturalist-indrecomp-truthrem075-tpow1-seed3-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 3, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c102-v12-deep-sea-indrecomp-truthrem075-tpow1-seed4-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 1, seed 4, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c102-v11-naturalist-indrecomp-truthrem075-tpow05-seed3-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth timepow 0.5, seed 3, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c101-v07-timeline-indrecomp-truthrem075-seed1-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth, seed 1, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c101-v10-orbit-indrecomp-truthrem075-seed1-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth, seed 1, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c101-v09-reef-indrecomp-truthrem075-seed1-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth, seed 1, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c101-v11-naturalist-indrecomp-truthrem075-seed2-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth, seed 2, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c101-v12-deep-sea-indrecomp-truthrem075-seed3-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth, seed 3, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c101-v11-naturalist-indrecomp-statesplit-truthrem075-seed2-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.75 ref truth, seed 2, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c100-v07-timeline-indrecomp-tblend-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.35, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target timeline-illustration.
- `c100-v08-transit-indrecomp-tblend-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.35, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target transit-illustration.
- `c100-v09-reef-indrecomp-tblend-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.35, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target reef-topic.
- `c100-v10-orbit-indrecomp-tblend-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.35, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target orbit-topic.
- `c100-v11-naturalist-indrecomp-tblend-mid60-seed1-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, seed 1, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c100-v11-naturalist-indrecomp-statesplit-mid60-seed1-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, seed 1, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.6 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c100-v12-deep-sea-indrecomp-tblend-mid25-seed1-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c100-v12-deep-sea-indrecomp-statesplit-mid25-seed2-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.25 width 0.18, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c99-v11-naturalist-indrecomp-base-rem025-mid50-seed0-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.5 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c99-v11-naturalist-indrecomp-tblend-init02-rem025-mid50-seed0-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.5 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c99-v11-naturalist-indrecomp-statesplit-init02-rem025-mid50-seed0-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.5 width 0.24, mid-time 0.68, endpoint anchors 0.14/0.55, clean target naturalist-plate.
- `c99-v12-deep-sea-indrecomp-base-rem050-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c99-v12-deep-sea-indrecomp-tblend-init02-rem050-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c99-v12-deep-sea-indrecomp-statesplit-init02-rem050-mid35-seed1-s12000`: done - Learned clean page-state proof: the training target independently recomposes source and target page regions before resolving to a separate clean page state at the midpoint; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.65, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.68, endpoint anchors 0.14/0.55, clean target deep-sea-lab.
- `c98-v11-naturalist-movereveal-base-rem025-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target moves the source layer while the separate clean target page eases into place; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, endpoint anchors 0.12/0.5, clean target naturalist-plate.
- `c98-v11-naturalist-movereveal-tblend-init02-rem025-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target moves the source layer while the separate clean target page eases into place; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, endpoint anchors 0.12/0.5, clean target naturalist-plate.
- `c98-v11-naturalist-movereveal-tblend-init02-rem025-mid50-seed0-s12000`: done - Learned clean page-state proof: the training target moves the source layer while the separate clean target page eases into place; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.5 width 0.24, mid-time 0.65, endpoint anchors 0.12/0.5, clean target naturalist-plate.
- `c98-v11-naturalist-movereveal-statesplit-init02-rem025-mid50-seed0-s12000`: done - Learned clean page-state proof: the training target moves the source layer while the separate clean target page eases into place; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.5 width 0.24, mid-time 0.65, endpoint anchors 0.12/0.5, clean target naturalist-plate.
- `c98-v12-deep-sea-movereveal-base-rem050-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target moves the source layer while the separate clean target page eases into place; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, endpoint anchors 0.12/0.5, clean target deep-sea-lab.
- `c98-v12-deep-sea-movereveal-tblend-init02-rem050-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target moves the source layer while the separate clean target page eases into place; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, endpoint anchors 0.12/0.5, clean target deep-sea-lab.
- `c98-v12-deep-sea-movereveal-tblend-init02-rem050-mid20-seed1-s12000`: done - Learned clean page-state proof: the training target moves the source layer while the separate clean target page eases into place; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, endpoint anchors 0.12/0.5, clean target deep-sea-lab.
- `c98-v12-deep-sea-movereveal-statesplit-init02-rem050-mid20-seed1-s12000`: done - Learned clean page-state proof: the training target moves the source layer while the separate clean target page eases into place; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, endpoint anchors 0.12/0.5, clean target deep-sea-lab.
- `c97-v11-naturalist-statesplit-init02-rem025-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, endpoint anchors 0.12/0.5, clean target naturalist-plate.
- `c97-v11-naturalist-statesplit-init02-rem025-mid50-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.5 width 0.24, mid-time 0.65, endpoint anchors 0.12/0.5, clean target naturalist-plate.
- `c97-v11-naturalist-statesplit-init05-rem025-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.05, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, endpoint anchors 0.12/0.5, clean target naturalist-plate.
- `c97-v11-naturalist-statesplit-init02-rem050-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, endpoint anchors 0.12/0.5, clean target naturalist-plate.
- `c97-v11-naturalist-statesplit-noscoord-init02-rem025-mid50-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.5 width 0.24, mid-time 0.65, endpoint anchors 0.12/0.5, clean target naturalist-plate.
- `c97-v12-deep-sea-statesplit-init02-rem050-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, endpoint anchors 0.12/0.5, clean target deep-sea-lab.
- `c97-v12-deep-sea-statesplit-init02-rem050-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, endpoint anchors 0.12/0.5, clean target deep-sea-lab.
- `c97-v12-deep-sea-statesplit-init05-rem050-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.05, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, endpoint anchors 0.12/0.5, clean target deep-sea-lab.
- `c97-v12-deep-sea-statesplit-init02-rem100-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 1, source-coord features, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, endpoint anchors 0.12/0.5, clean target deep-sea-lab.
- `c97-v12-deep-sea-statesplit-init02-rem050-mid20-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, decoder state-split target-scale 0 target-h160, target-state canvas always init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, endpoint anchors 0.12/0.5, clean target deep-sea-lab.
- `c96-v11-naturalist-tblend-init02-rem025-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c96-v11-naturalist-tblend-init05-rem025-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.05, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c96-v11-naturalist-tblend-init02-rem025-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, seed 1, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c96-v11-naturalist-tblend-init02-rem050-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c96-v11-naturalist-tblend-init02-rem000-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c96-v12-deep-sea-tblend-init02-rem050-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c96-v12-deep-sea-tblend-init05-rem050-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.05, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c96-v12-deep-sea-tblend-init02-rem050-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c96-v12-deep-sea-tblend-init02-rem000-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c96-v12-deep-sea-tblend-init02-rem100-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 1, source-coord features, latent cross neighborhood r1px source, target-state canvas blend init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c95-v11-naturalist-tcanvas-gated-init02-rem025-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, target-state canvas gated init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c95-v11-naturalist-tcanvas-gated-init05-rem025-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, target-state canvas gated init 0.05, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c95-v11-naturalist-tcanvas-gated-init02-rem025-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, seed 1, source-coord features, latent cross neighborhood r1px source, target-state canvas gated init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c95-v11-naturalist-tcanvas-always-init02-rem025-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c95-v11-naturalist-tcanvas-gated-init02-rem050-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, target-state canvas gated init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c95-v12-deep-sea-tcanvas-gated-init02-rem050-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, target-state canvas gated init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c95-v12-deep-sea-tcanvas-gated-init05-rem050-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, target-state canvas gated init 0.05, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c95-v12-deep-sea-tcanvas-gated-init02-rem050-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-state canvas gated init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c95-v12-deep-sea-tcanvas-always-init02-rem050-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, target-state canvas always init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c95-v12-deep-sea-tcanvas-gated-init02-rem000-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-state canvas gated init 0.02, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c94-v11-naturalist-dualres-s050-rem025-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, decoder dual-residual target-scale 0.5 target-h96, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c94-v11-naturalist-dualres-s100-rem025-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, decoder dual-residual target-scale 1 target-h96, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c94-v11-naturalist-dualfused-s050-rem025-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, decoder dual-residual-fused target-scale 0.5 target-h96, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c94-v11-naturalist-dualgate-s100-rem025-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, decoder dual-gate target-scale 1 target-h96, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c94-v11-naturalist-latentboth-rem025-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px both, decoder single target-scale 0 target-h96, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c94-v12-deep-sea-dualres-s050-rem050-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, decoder dual-residual target-scale 0.5 target-h96, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c94-v12-deep-sea-dualres-s100-rem050-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, decoder dual-residual target-scale 1 target-h96, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c94-v12-deep-sea-dualfused-s050-rem050-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, decoder dual-residual-fused target-scale 0.5 target-h96, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c94-v12-deep-sea-dualgate-s100-rem050-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, decoder dual-gate target-scale 1 target-h96, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c94-v12-deep-sea-latentboth-rem050-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px both, decoder single target-scale 0 target-h96, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c93-v11-naturalist-rem025-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c93-v11-naturalist-rem025-mid35-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c93-v11-naturalist-rem050-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c93-v11-naturalist-rem050-mid35-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c93-v11-naturalist-rem050-mid50-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.5 width 0.16, mid-time 0.65, clean target naturalist-plate.
- `c93-v11-naturalist-rem100-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c93-v12-deep-sea-rem025-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.25, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c93-v12-deep-sea-rem050-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c93-v12-deep-sea-rem050-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target deep-sea-lab.
- `c93-v12-deep-sea-rem100-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-remnant contrast 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c92-v11-naturalist-plate-target60-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target naturalist-plate.
- `c92-v11-naturalist-plate-target60-mid20-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target naturalist-plate.
- `c92-v11-naturalist-plate-target60-mid20-seed2-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target naturalist-plate.
- `c92-v11-naturalist-plate-target60-mid20-seed4-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target naturalist-plate.
- `c92-v12-deep-sea-lab-target60-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c92-v12-deep-sea-lab-target60-mid20-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c92-v12-deep-sea-lab-target60-mid20-seed2-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c92-v12-deep-sea-lab-target60-mid20-seed4-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target deep-sea-lab.
- `c92-v11-naturalist-plate-target60-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target naturalist-plate.
- `c92-v12-deep-sea-lab-target60-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target deep-sea-lab.
- `c91-v9-reef-topic-target60-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target reef-topic.
- `c91-v9-reef-topic-target60-mid20-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target reef-topic.
- `c91-v9-reef-topic-target60-mid20-seed2-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target reef-topic.
- `c91-v9-reef-topic-target60-mid20-seed4-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target reef-topic.
- `c91-v10-orbit-topic-target60-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target orbit-topic.
- `c91-v10-orbit-topic-target60-mid20-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target orbit-topic.
- `c91-v10-orbit-topic-target60-mid20-seed2-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target orbit-topic.
- `c91-v10-orbit-topic-target60-mid20-seed4-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target orbit-topic.
- `c91-v9-reef-topic-target60-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target reef-topic.
- `c91-v10-orbit-topic-target60-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target orbit-topic.
- `c90-v7-timeline-illustration-target60-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target timeline-illustration.
- `c90-v7-timeline-illustration-target60-mid20-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target timeline-illustration.
- `c90-v7-timeline-illustration-target60-mid20-seed2-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target timeline-illustration.
- `c90-v7-timeline-illustration-target60-mid20-seed4-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target timeline-illustration.
- `c90-v8-transit-illustration-target60-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target transit-illustration.
- `c90-v8-transit-illustration-target60-mid20-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target transit-illustration.
- `c90-v8-transit-illustration-target60-mid20-seed2-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target transit-illustration.
- `c90-v8-transit-illustration-target60-mid20-seed4-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target transit-illustration.
- `c90-v7-timeline-illustration-target60-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target timeline-illustration.
- `c90-v8-transit-illustration-target60-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target transit-illustration.
- `c89-v5-changed-unboxed-target60-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target changed-unboxed.
- `c89-v5-changed-unboxed-target60-mid20-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target changed-unboxed.
- `c89-v5-changed-unboxed-target60-mid20-seed2-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target changed-unboxed.
- `c89-v5-changed-unboxed-target60-mid20-seed4-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target changed-unboxed.
- `c89-v6-changed-callout-target60-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target changed-callout.
- `c89-v6-changed-callout-target60-mid20-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target changed-callout.
- `c89-v6-changed-callout-target60-mid20-seed2-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target changed-callout.
- `c89-v6-changed-callout-target60-mid20-seed4-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target changed-callout.
- `c89-v5-changed-unboxed-target60-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target changed-unboxed.
- `c89-v6-changed-callout-target60-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target changed-callout.
- `c88-v3-unboxed-target60-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target unboxed-columns.
- `c88-v3-unboxed-target60-mid20-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target unboxed-columns.
- `c88-v3-unboxed-target60-mid20-seed2-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target unboxed-columns.
- `c88-v3-unboxed-target60-mid20-seed4-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target unboxed-columns.
- `c88-v4-callout-target60-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target callout-map.
- `c88-v4-callout-target60-mid20-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target callout-map.
- `c88-v4-callout-target60-mid20-seed2-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target callout-map.
- `c88-v4-callout-target60-mid20-seed4-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target callout-map.
- `c88-v3-unboxed-target60-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target unboxed-columns.
- `c88-v4-callout-target60-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target callout-map.
- `c87-v1-c32h160-target60-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target right-diagram.
- `c87-v1-c32h160-target60-mid20-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target right-diagram.
- `c87-v1-c32h160-target60-mid20-seed2-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target right-diagram.
- `c87-v1-c32h160-target60-mid20-seed4-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target right-diagram.
- `c87-v2-c32h160-target60-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target stacked.
- `c87-v2-c32h160-target60-mid20-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target stacked.
- `c87-v2-c32h160-target60-mid20-seed2-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target stacked.
- `c87-v2-c32h160-target60-mid20-seed4-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65, clean target stacked.
- `c87-v1-c32h160-target60-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target right-diagram.
- `c87-v2-c32h160-target60-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65, clean target stacked.
- `c86-clean-c32h160-target60-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65.
- `c86-clean-c32h160-target60-mid20-seed1-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65.
- `c86-clean-c32h160-target60-mid20-seed2-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65.
- `c86-clean-c32h160-target60-mid20-seed4-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65.
- `c86-clean-c32h160-target80-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.8, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65.
- `c86-clean-c32h160-target60-mid35-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.22, mid-time 0.65.
- `c86-clean-c24h128-target60-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65.
- `c86-clean-c40h192-target60-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65.
- `c86-clean-c32h160-context8-target60-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65.
- `c86-clean-c32h160-dualres-target60-mid20-seed0-s12000`: done - Learned clean page-state proof: the training target is a separate clean page state at the midpoint, then loops back to the source; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.25 target-h80, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.18, mid-time 0.65.
- `c85-gatelearn025-b025-r275-a110-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source gate learned, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c85-gatelearn030-b025-r275-a110-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source gate learned, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c85-gatelearn040-b025-r275-a110-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source gate learned, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c85-gatelearn045-b025-r275-a110-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source gate learned, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c85-gatelearn035-b025-r275-a110-seed0-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source gate learned, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c85-gatelearn035-b025-r275-a110-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source gate learned, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c85-gatelearn035-b025-r275-a110-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source gate learned, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c85-gatelearn035-b025-r275-a110-seed3-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 3, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source gate learned, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c85-gatelearn035-b025-r275-a110-seed5-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 5, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source gate learned, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c85-gatelearn035-b025-r300-a110-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 3 source gate learned, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c84-gateedge-b025-r275-a110-seed0-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source gate edge, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c84-gateedge-b025-r275-a110-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source gate edge, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c84-gateedge-b025-r275-a110-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source gate edge, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c84-gateedge-b025-r275-a110-seed5-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 5, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source gate edge, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c84-gateedge-b025-r325-a110-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 3.25 source gate edge, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c84-gateedge-b030-r275-a110-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.3 residual 2.75 source gate edge, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c84-gatelearn035-b025-r275-a110-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source gate learned, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c84-gatelearn050-b025-r275-a110-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source gate learned, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c84-gatelearn035-b025-r325-a110-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 3.25 source gate learned, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c84-gateedge-b020-r275-a110-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.2 residual 2.75 source gate edge, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c83-nctx-b025-r275-a110-seed0-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c83-nctx-b025-r275-a110-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c83-nctx-b025-r275-a110-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c83-nctx-b025-r275-a110-seed3-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 3, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c83-nctx-b025-r275-a110-seed5-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 5, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c83-nctx-b020-r275-a110-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.2 residual 2.75 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c83-nctx-b030-r275-a110-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.3 residual 2.75 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c83-nctx-b025-r325-a110-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 3.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c83-nctx-b025-r275-a100-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c83-nctx-b025-r275-a115-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.15, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c82-nctx-b025-r250-a110-seed0-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c82-nctx-b025-r250-a110-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c82-nctx-b025-r250-a110-seed3-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 3, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c82-nctx-b025-r250-a110-seed5-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 5, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c82-nctx-b025-r200-a120-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.2, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c82-nctx-b025-r225-a115-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.15, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c82-nctx-b025-r300-a105-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.05, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 3 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c82-nctx-b025-r300-a110-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 3 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c82-nctx-b025-r225-a115-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.15, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c82-nctx-b025-r275-a110-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.75 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c81-nctx-b025-r200-a110-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c81-nctx-b025-r250-a100-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c81-nctx-b025-r250-a110-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c81-nctx-b025-r300-a100-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 3 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c81-nctx-b020-r250-a100-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.2 residual 2.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c81-nctx-b030-r250-a100-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.3 residual 2.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c81-nctx-b025-r250-a100-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c81-nctx-b025-r250-a100-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c81-nctx-b025-r250-a110-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1.1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c81-nctx-b050-r400-a100-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.5 residual 4 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c80-rgbbase025-res100-c8-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, rgb neural texture skip base 0.25 residual 1 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c80-rgbbase025-res200-c8-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, rgb neural texture skip base 0.25 residual 2 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c80-rgbbase025-res400-c8-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, rgb neural texture skip base 0.25 residual 4 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c80-rgbbase050-res200-c8-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, rgb neural texture skip base 0.5 residual 2 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c80-rgbbase050-res400-c8-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, rgb neural texture skip base 0.5 residual 4 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c80-rgbbase075-res200-c8-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, rgb neural texture skip base 0.75 residual 2 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c80-rgbbase025-res200-c8-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, rgb neural texture skip base 0.25 residual 2 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c80-rgbbase050-res400-c8-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, rgb neural texture skip base 0.5 residual 4 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c80-rgbbase050-res400-c8-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, rgb neural texture skip base 0.5 residual 4 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c80-rgbbase025-res200-nocontext-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 0.25 residual 2 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c79-rgbskip-s025-c8-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, rgb neural texture skip base 1 residual 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c79-rgbskip-s025-c8-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, rgb neural texture skip base 1 residual 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c79-rgbskip-s025-c8-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, rgb neural texture skip base 1 residual 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c79-rgbskip-s050-c8-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, rgb neural texture skip base 1 residual 0.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c79-rgbskip-s050-c8-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, rgb neural texture skip base 1 residual 0.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c79-rgbskip-s050-c8-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, rgb neural texture skip base 1 residual 0.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c79-rgbskip-s100-c8-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, rgb neural texture skip base 1 residual 1 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c79-rgbskip-s100-c8-seed5-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 5, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, rgb neural texture skip base 1 residual 1 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c79-rgbskip-s050-nocontext-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 1 residual 0.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c79-rgbskip-s050-nocontext-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, rgb neural texture skip base 1 residual 0.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c78-fused-s025-c8-h80-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual-fused target-scale 0.25 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c78-fused-s025-c8-h80-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual-fused target-scale 0.25 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c78-fused-s025-c8-h80-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual-fused target-scale 0.25 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c78-fused-s025-c8-h80-seed5-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 5, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual-fused target-scale 0.25 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c78-fused-s025-c8-h80-seed6-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 6, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual-fused target-scale 0.25 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c78-fused-s035-c8-h80-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual-fused target-scale 0.35 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c78-fused-s035-c8-h80-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual-fused target-scale 0.35 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c78-fused-s035-c8-h80-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual-fused target-scale 0.35 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c78-fused-s025-c8-h64-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual-fused target-scale 0.25 target-h64, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c78-fused-s025-c8-h64-seed5-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 5, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual-fused target-scale 0.25 target-h64, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c77-map-s025-c8-h80-seed5-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 5, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.25 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c77-map-s025-c8-h80-seed6-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 6, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.25 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c77-map-s025-c8-h80-seed7-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 7, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.25 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c77-map-s025-c8-h80-seed8-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 8, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.25 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c77-map-s035-c8-h64-seed5-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 5, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.35 target-h64, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c77-map-s035-c8-h64-seed6-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 6, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.35 target-h64, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c77-map-s035-c8-h64-seed7-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 7, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.35 target-h64, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c77-map-s035-c8-h64-seed8-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 8, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.35 target-h64, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c77-stab-s025-c8-h80-lr007-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.25 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c77-stab-s025-c8-h80-clip025-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.25, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.25 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c76-dualres-s025-c8-th48-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.25 target-h48, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c76-dualres-s025-c8-th48-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.25 target-h48, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c76-dualres-s025-c8-th48-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.25 target-h48, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c76-dualres-s035-c8-th64-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.35 target-h64, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c76-dualres-s035-c8-th64-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.35 target-h64, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c76-dualres-s035-c8-th64-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.35 target-h64, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c76-dualres-s050-nocontext-th64-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, decoder dual-residual target-scale 0.5 target-h64, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c76-dualres-s050-nocontext-th64-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, decoder dual-residual target-scale 0.5 target-h64, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c76-dualres-s050-nocontext-th64-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, decoder dual-residual target-scale 0.5 target-h64, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c76-dualres-s025-nocontext-th64-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, decoder dual-residual target-scale 0.25 target-h64, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c75-dualres-s025-c8-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.25 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c75-dualres-s025-c8-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.25 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c75-dualres-s025-c8-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.25 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c75-dualres-s050-c8-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.5 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c75-dualres-s050-c8-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.5 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c75-dualres-s050-c8-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, decoder dual-residual target-scale 0.5 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c75-dualres-s050-c4-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 source, decoder dual-residual target-scale 0.5 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c75-dualres-s050-c4-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 source, decoder dual-residual target-scale 0.5 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c75-dualres-s050-c4-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 source, decoder dual-residual target-scale 0.5 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c75-dualres-s050-nocontext-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, decoder dual-residual target-scale 0.5 target-h80, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c74-latentboth-c8-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px both, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c74-latentboth-c8-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px both, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c74-latentboth-c8-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px both, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c74-latenttarget-c8-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px target, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c74-latenttarget-c8-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px target, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c74-latentboth-c4-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px both, context c4 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c74-latentboth-c4-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px both, context c4 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c74-latentboth-c4-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px both, context c4 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c74-latentboth-nocontext-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px both, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c74-latentboth-nocontext-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px both, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c73-c8target-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 target, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c73-c8target-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 target, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c73-c8target-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 target, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c73-c8both-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 both, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c73-c8both-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 both, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c73-c8both-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 both, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c73-c4target-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 target, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c73-c4target-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 target, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c73-c4both-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 both, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c73-c4both-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 both, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c72-c8mid05-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.05 width 0.24, mid-time 0.6.
- `c72-c8mid05-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.05 width 0.24, mid-time 0.6.
- `c72-c8mid05-seed3-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 3, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.05 width 0.24, mid-time 0.6.
- `c72-c8mid05-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.05 width 0.24, mid-time 0.6.
- `c72-c8mid05-seed6-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 6, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.05 width 0.24, mid-time 0.6.
- `c72-c4mid15-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.15 width 0.24, mid-time 0.6.
- `c72-c4mid15-seed3-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 3, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.15 width 0.24, mid-time 0.6.
- `c72-c4mid15-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.15 width 0.24, mid-time 0.6.
- `c72-c4mid15-seed5-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 5, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.15 width 0.24, mid-time 0.6.
- `c72-c4mid20-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.24, mid-time 0.6.
- `c71-mid05-c8s025-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.05 width 0.24, mid-time 0.6.
- `c71-mid075-c8s025-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.075 width 0.24, mid-time 0.6.
- `c71-mid15-c8s025-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.15 width 0.24, mid-time 0.6.
- `c71-mid05-c8s025-seed5-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 5, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.05 width 0.24, mid-time 0.6.
- `c71-mid10-c8s025-seed5-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 5, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.1 width 0.24, mid-time 0.6.
- `c71-mid10-c8s025-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.1 width 0.24, mid-time 0.6.
- `c71-mid10-c4s025-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.1 width 0.24, mid-time 0.6.
- `c71-mid10-c4s025-seed3-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 3, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.1 width 0.24, mid-time 0.6.
- `c71-mid10-c4s025-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.1 width 0.24, mid-time 0.6.
- `c71-mid15-c4s025-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.15 width 0.24, mid-time 0.6.
- `c70-mid10-c8s025-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.1 width 0.24, mid-time 0.6.
- `c70-mid20-c8s025-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.24, mid-time 0.6.
- `c70-mid35-c8s025-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.35 width 0.24, mid-time 0.6.
- `c70-mid20w012-c8s025-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.12, mid-time 0.6.
- `c70-mid20-c8s025-seed5-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 5, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.24, mid-time 0.6.
- `c70-mid20-c8s025-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.24, mid-time 0.6.
- `c70-mid20-c4s025-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.24, mid-time 0.6.
- `c70-mid20-c4s025-seed3-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 3, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 source, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.24, mid-time 0.6.
- `c70-mid20-c8s025-target45-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.45, direct target-mid sampling ratio 0.2 width 0.24, mid-time 0.6.
- `c70-mid20-c8s025-text65-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, text-weighted, target-side reflow sampling ratio 0.6, direct target-mid sampling ratio 0.2 width 0.24, mid-time 0.6.
- `c69-context8s025-cross1-target60-c32h160-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c69-context8s025-cross1-target60-c32h160-seed3-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 3, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c69-context8s025-cross1-target60-c32h160-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c69-context8s025-cross1-target60-c32h160-seed5-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 5, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c69-context16s050-cross1-target60-c32h160-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c16 scale 0.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c69-context16s050-cross1-target60-c32h160-seed3-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 3, source-coord features, latent cross neighborhood r1px source, context c16 scale 0.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c69-context16s050-cross1-target60-c32h160-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c16 scale 0.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c69-context8s050-cross1-target60-c32h160-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c69-context4s025-cross1-target60-c32h160-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c4 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c69-context12s025-cross1-target60-c32h160-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c12 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c68-context16s025-cross1-target60-c32h160-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c16 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c68-context16s050-cross1-target60-c32h160-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c16 scale 0.5 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c68-context32s025-cross1-target60-c32h160-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c32 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c68-context8s025-cross1-target60-c32h160-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, context c8 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c68-context16s025-cross05-target60-c32h160-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r0.5px source, context c16 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c68-context16s025-cross1-target60-c32h160-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c16 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c68-context16s025-cross1-target60-c32h160-seed3-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 3, source-coord features, latent cross neighborhood r1px source, context c16 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c68-context16s025-cross1-target60-c32h160-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, context c16 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c68-context16s025-cross1-target60-c24h128-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, context c16 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c68-context16s025-grid1-target60-c24h128-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent grid neighborhood r1px source, context c16 scale 0.25 source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c67-neighbor-cross1-target60-c32h160-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c67-neighbor-cross1-target60-c32h160-seed3-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 3, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c67-neighbor-cross1-target60-c32h160-seed4-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c67-neighbor-cross1-target55-c32h160-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.55, mid-time 0.6.
- `c67-neighbor-cross1-target65-c32h160-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.65, mid-time 0.6.
- `c67-neighbor-cross05-target60-c32h160-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r0.5px source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c67-neighbor-cross15-target60-c32h160-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1.5px source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c67-neighbor-grid1-target60-c32h160-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent grid neighborhood r1px source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c67-neighbor-cross1-target60-b196-c32h160-seed1-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c67-neighbor-cross1-target60-c40h192-seed1-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c66-neighbor-cross1-target60-c24h128-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c66-neighbor-cross2-target60-c24h128-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r2px source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c66-neighbor-grid1-target60-c24h128-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent grid neighborhood r1px source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c66-neighbor-grid2-target60-c24h128-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent grid neighborhood r2px source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c66-neighbor-cross1-target60-curr50-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, mid-time 0.6, motion curriculum 0.5/0.
- `c66-neighbor-grid1-target60-curr50-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, latent grid neighborhood r1px source, target-side reflow sampling ratio 0.6, mid-time 0.6, motion curriculum 0.5/0.
- `c66-neighbor-cross1-target60-c24h128-seed3-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 3, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c66-neighbor-cross1-target60-c24h128-seed4-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 4, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c66-neighbor-cross1-target75-c32h160-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.75, mid-time 0.6.
- `c66-neighbor-cross1-target60-c32h160-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, latent cross neighborhood r1px source, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c65-sourcecoord-target60-c24h128-end25-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6, endpoint anchors 0.25/0.6.
- `c65-sourcecoord-target60-c24h128-end50-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6, endpoint anchors 0.5/0.6.
- `c65-sourcecoord-target60-c24h128-end75-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6, endpoint anchors 0.75/0.6.
- `c65-sourcecoord-target60-c24h128-end50-curr50-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6, motion curriculum 0.5/0, endpoint anchors 0.5/0.6.
- `c65-sourcecoord-target60-c24h128-end50-curr25s025-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6, motion curriculum 0.25/0.25, endpoint anchors 0.5/0.6.
- `c65-sourcecoord-target60-c24h128-end50-seed3-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 3, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6, endpoint anchors 0.5/0.6.
- `c65-sourcecoord-target60-c24h128-end50-seed4-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 4, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6, endpoint anchors 0.5/0.6.
- `c65-sourcecoord-target60-c24h128-end50-seed5-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 5, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6, endpoint anchors 0.5/0.6.
- `c65-sourcecoord-target75-c32h160-end50-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, target-side reflow sampling ratio 0.75, mid-time 0.6, endpoint anchors 0.5/0.6.
- `c65-sourcecoord-target60-c32h160-end50-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6, endpoint anchors 0.5/0.6.
- `c64-sourcecoord-target60-c24h128-curr25-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6, motion curriculum 0.25/0.
- `c64-sourcecoord-target60-c24h128-curr50-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6, motion curriculum 0.5/0.
- `c64-sourcecoord-target60-c24h128-curr25s025-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6, motion curriculum 0.25/0.25.
- `c64-sourcecoord-target60-c24h128-curr50s025-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6, motion curriculum 0.5/0.25.
- `c64-sourcecoord-target60-c24h128-curr25s025-seed3-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 3, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6, motion curriculum 0.25/0.25.
- `c64-sourcecoord-target60-c24h128-curr25s025-seed4-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 4, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6, motion curriculum 0.25/0.25.
- `c64-sourcecoord-target60-c24h128-curr25s025-seed5-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 5, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6, motion curriculum 0.25/0.25.
- `c64-sourcecoord-target65-c24h128-curr25s025-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.65, mid-time 0.6, motion curriculum 0.25/0.25.
- `c64-sourcecoord-target60-c32h160-curr25s025-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6, motion curriculum 0.25/0.25.
- `c64-sourcecoord-target75-c32h160-curr25s025-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, target-side reflow sampling ratio 0.75, mid-time 0.6, motion curriculum 0.25/0.25.
- `c63-sourcecoord-target60-c24h128-seed3-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 3, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c63-sourcecoord-target60-c24h128-seed4-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 4, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c63-sourcecoord-target60-c24h128-seed5-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 5, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c63-sourcecoord-target50-c24h128-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c63-sourcecoord-target55-c24h128-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.55, mid-time 0.6.
- `c63-sourcecoord-target65-c24h128-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.65, mid-time 0.6.
- `c63-sourcecoord-target60-c24h128-seed2-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 10000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c63-sourcecoord-target60-c24h128-seed2-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c63-sourcecoord-target60-c24h128-edge06-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c63-sourcecoord-target60-c24h128-edge12-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c62-sourcecoord-target60-b196-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c62-sourcecoord-target60-b196-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c62-sourcecoord-target60-b196-seed1-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, seed 1, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c62-sourcecoord-target60-b196-seed2-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c62-sourcecoord-target60-b196-seed3-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, seed 3, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c62-sourcecoord-target60-b196-edge06-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c62-sourcecoord-target60-b196-edge12-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c62-sourcecoord-target60-c24h128-seed1-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 1, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c62-sourcecoord-target60-c24h128-seed2-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c62-sourcecoord-target60-c24h128-b196-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 10000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c61-sourcecoord-target55-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.55, mid-time 0.6.
- `c61-sourcecoord-target65-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.65, mid-time 0.6.
- `c61-sourcecoord-target60-seed1-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c61-sourcecoord-target60-seed2-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c61-sourcecoord-target60-flow08-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 14000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c61-sourcecoord-target60-b196-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c61-sourcecoord-target55-c24h128-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.55, mid-time 0.6.
- `c61-sourcecoord-target60-c24h128-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c61-sourcecoord-target60-cosine-c32h160-s16000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 16000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c61-sourcecoord-target60-lr007-c32h160-s16000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 16000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c60-sourcecoord-target60-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.6, mid-time 0.6.
- `c60-sourcecoord-target70-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.7, mid-time 0.6.
- `c60-sourcecoord-target80-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.8, mid-time 0.6.
- `c60-sourcecoord-target90-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.9, mid-time 0.6.
- `c60-sourcecoord-target100-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling, mid-time 0.6.
- `c60-sourcecoord-target75-seed1-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, target-side reflow sampling ratio 0.75, mid-time 0.6.
- `c60-sourcecoord-target75-seed2-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, source-coord features, target-side reflow sampling ratio 0.75, mid-time 0.6.
- `c60-sourcecoord-target75-b196-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.75, mid-time 0.6.
- `c60-sourcecoord-target75-c24h128-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.75, mid-time 0.6.
- `c60-sourcecoord-target75-c40h192-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.75, mid-time 0.6.
- `c59-oracleflow-target75-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.75, mid-time 0.6, oracle layout flow.
- `c59-oracleflow-target50-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.5, mid-time 0.6, oracle layout flow.
- `c59-oracleflow-weightonly-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling, mid-time 0.6, oracle layout flow.
- `c59-oracleflow-target75-c24h128-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.75, mid-time 0.6, oracle layout flow.
- `c59-oracleflow-target75-nosrc-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, target-side reflow sampling ratio 0.75, mid-time 0.6, oracle layout flow.
- `c59-oracleflow-target75-freq12-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq12, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.75, mid-time 0.6, oracle layout flow.
- `c58-flow020-target50-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.2, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c58-flow028-target50-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.28, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c58-flow035-target50-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.35, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c58-flow035-weightonly-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.35, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling, mid-time 0.6.
- `c58-flow045-target50-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.45, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c58-flowsup025-w0025-target50-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.25, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.5, mid-time 0.6, flow supervision 0.025.
- `c58-flowsup035-w0025-target50-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.35, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.5, mid-time 0.6, flow supervision 0.025.
- `c58-flowsup035-w005-target50-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.35, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.5, mid-time 0.6, flow supervision 0.05.
- `c58-flowsup035-w0025-weightonly-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.35, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling, mid-time 0.6, flow supervision 0.025.
- `c58-flowsup045-w0025-target50-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.45, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.5, mid-time 0.6, flow supervision 0.025.
- `c57-sourcecoord-target50-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c57-sourcecoord-target75-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.75, mid-time 0.6.
- `c57-sourcecoord-weightonly-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling, mid-time 0.6.
- `c57-sourcecoord-target-b196-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling, mid-time 0.6.
- `c57-sourcecoord-weight-b196-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling, mid-time 0.6.
- `c57-sourcecoord-target50-freq12-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq12, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c57-sourcecoord-target50-c24h128-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 12000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c57-sourcecoord-target50-flow08-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 14000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c57-sourcecoord-target50-seed1-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, source-coord features, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c57-sourcecoord-weightonly-train1920-s13000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 13000 steps, freq10, grad clip 0.5, source-coord features, target-side reflow sampling, mid-time 0.6.
- `c56-detail8-025-target50-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, residual detail c8/h96 scale 0.25, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c56-detail8-050-target50-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, residual detail c8/h96 scale 0.5, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c56-detail16-025-target50-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, residual detail c16/h96 scale 0.25, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c56-detail16h128-025-target50-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, residual detail c16/h128 scale 0.25, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c56-detail8-025-weightonly-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, residual detail c8/h96 scale 0.25, target-side reflow sampling, mid-time 0.6.
- `c56-detail16-025-weightonly-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, residual detail c16/h96 scale 0.25, target-side reflow sampling, mid-time 0.6.
- `c56-detail8-0125-weightonly-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, residual detail c8/h96 scale 0.125, target-side reflow sampling, mid-time 0.6.
- `c56-detail8-025-target-b196-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, residual detail c8/h96 scale 0.25, target-side reflow sampling, mid-time 0.6.
- `c56-detail16-025-target-b196-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, residual detail c16/h96 scale 0.25, target-side reflow sampling, mid-time 0.6.
- `c56-detail8-025-weightonly-train1920-s13000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 13000 steps, freq10, grad clip 0.5, residual detail c8/h96 scale 0.25, target-side reflow sampling, mid-time 0.6.
- `c55-reflow-pair0025-w050-weightonly-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, target-side reflow sampling, paired target loss ratio 0.025 weight 0.5, mid-time 0.6.
- `c55-reflow-pair005-w050-weightonly-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, target-side reflow sampling, paired target loss ratio 0.05 weight 0.5, mid-time 0.6.
- `c55-reflow-pair009-w050-weightonly-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, target-side reflow sampling, paired target loss ratio 0.09 weight 0.5, mid-time 0.6.
- `c55-reflow-pair005-w025-weightonly-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, target-side reflow sampling, paired target loss ratio 0.05 weight 0.25, mid-time 0.6.
- `c55-reflow-pair005-w100-weightonly-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, target-side reflow sampling, paired target loss ratio 0.05 weight 1, mid-time 0.6.
- `c55-reflow-pair005-w050-target-s025-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, target-side reflow sampling ratio 0.25, paired target loss ratio 0.05 weight 0.5, mid-time 0.6.
- `c55-reflow-pair005-w050-target-s050-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, target-side reflow sampling ratio 0.5, paired target loss ratio 0.05 weight 0.5, mid-time 0.6.
- `c55-reflow-pair005-w050-b196-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, target-side reflow sampling, paired target loss ratio 0.05 weight 0.5, mid-time 0.6.
- `c55-reflow-pair009-w050-b196-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, target-side reflow sampling, paired target loss ratio 0.09 weight 0.5, mid-time 0.6.
- `c55-reflow-pair005-w050-train1920-s13000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 13000 steps, freq10, grad clip 0.5, target-side reflow sampling, paired target loss ratio 0.05 weight 0.5, mid-time 0.6.
- `c54-reflow-grad025-r00625-target-s050-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, gradient 0.25@0.0625, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c54-reflow-grad050-r00625-target-s050-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, gradient 0.5@0.0625, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c54-reflow-grad025-r0125-target-s050-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, gradient 0.25@0.125, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c54-reflow-grad050-r0125-target-s050-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, gradient 0.5@0.125, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c54-reflow-grad025-r00625-weightonly-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, gradient 0.25@0.0625, target-side reflow sampling, mid-time 0.6.
- `c54-reflow-grad050-r00625-weightonly-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, gradient 0.5@0.0625, target-side reflow sampling, mid-time 0.6.
- `c54-reflow-grad025-r0125-weightonly-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, gradient 0.25@0.125, target-side reflow sampling, mid-time 0.6.
- `c54-reflow-grad025-r00625-target-full-b196-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, gradient 0.25@0.0625, target-side reflow sampling, mid-time 0.6.
- `c54-reflow-grad050-r00625-target-full-b196-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, gradient 0.5@0.0625, target-side reflow sampling, mid-time 0.6.
- `c54-reflow-grad025-r00625-train1920-weightonly-s13000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 13000 steps, freq10, grad clip 0.5, gradient 0.25@0.0625, target-side reflow sampling, mid-time 0.6.
- `c49-reflow-target-mid60-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c49-reflow-target-mid80-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.8.
- `c49-reflow-target-mid60-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c49-reflow-target-lr007-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c49-reflow-target-time6-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c49-reflow-target-edge18-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c49-reflow-target-textw-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, text-weighted, target-side reflow sampling, mid-time 0.6.
- `c49-reflow-target-b196-c32h160-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c49-reflow-target-train1920-c24h128-s11000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 11000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c49-reflow-target-train1920-c32h160-s11000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 11000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c50-reflow-ablate-target-sample-only`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c50-reflow-ablate-target-weight-only`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c50-reflow-ablate-midtime-only`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, mid-time 0.7.
- `c50-reflow-target-mid40-c32h160-s11000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 11000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.4.
- `c50-reflow-target-mid90n-c32h160-s11000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 11000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.9.
- `c50-reflow-target-freq12-c32h160-s11000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 11000 steps, freq12, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c50-reflow-target-c40h192-s11000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 11000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c50-reflow-target-cosine-c32h160-s11000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 11000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c50-reflow-target-clip1-c32h160-s11000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 11000 steps, freq10, grad clip 1, target-side reflow sampling, mid-time 0.6.
- `c50-reflow-target-train1920-textw-c24h128-s11000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 11000 steps, freq10, grad clip 0.5, text-weighted, target-side reflow sampling, mid-time 0.6.
- `c53-reflow-weightonly-l1-025-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, L1 0.25, target-side reflow sampling, mid-time 0.6.
- `c53-reflow-weightonly-l1-050-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, L1 0.5, target-side reflow sampling, mid-time 0.6.
- `c53-reflow-weightonly-l1-100-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, L1 1, target-side reflow sampling, mid-time 0.6.
- `c53-reflow-target-s050-l1-025-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, L1 0.25, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c53-reflow-target-s050-l1-050-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, L1 0.5, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c53-reflow-target-full-l1-025-b196-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, L1 0.25, target-side reflow sampling, mid-time 0.6.
- `c53-reflow-target-full-l1-050-b196-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, L1 0.5, target-side reflow sampling, mid-time 0.6.
- `c53-reflow-target-full-l1-025-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, L1 0.25, target-side reflow sampling, mid-time 0.6.
- `c53-reflow-target-full-l1-050-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, L1 0.5, target-side reflow sampling, mid-time 0.6.
- `c53-reflow-weightonly-l1-025-train1920-s13000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 13000 steps, freq10, grad clip 0.5, L1 0.25, target-side reflow sampling, mid-time 0.6.
- `c52-reflow-target-s025-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, target-side reflow sampling ratio 0.25, mid-time 0.6.
- `c52-reflow-target-s050-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, target-side reflow sampling ratio 0.5, mid-time 0.6.
- `c52-reflow-target-s075-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, target-side reflow sampling ratio 0.75, mid-time 0.6.
- `c52-reflow-weightonly-c32h160-s16000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 16000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c52-reflow-weightonly-c32h160-s18000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 18000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c52-reflow-weightonly-b196-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c52-reflow-weightonly-b196-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c52-reflow-weightonly-train1920-c32h160-s13000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 13000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c52-reflow-weightonly-c32h160-s14000-seed1`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, target-side reflow sampling, mid-time 0.6.
- `c52-reflow-weightonly-c32h160-s14000-seed2`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 2, target-side reflow sampling, mid-time 0.6.
- `c52-reflow-target-b196-c32h160-s10000-seed2`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, seed 2, target-side reflow sampling, mid-time 0.6.
- `c51-reflow-target-b196-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c51-reflow-target-b196-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c51-reflow-target-b229-c32h160-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c51-reflow-target-mid60-c32h160-s16000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 16000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c51-reflow-target-mid60-c32h160-s18000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 18000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c51-reflow-weightonly-b196-c32h160-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c51-reflow-weightonly-c32h160-s14000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c51-reflow-target-train1920-c32h160-s13000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 13000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c51-reflow-target-train1920-c32h160-s15000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 15000 steps, freq10, grad clip 0.5, target-side reflow sampling, mid-time 0.6.
- `c51-reflow-target-b196-c32h160-s10000-seed1`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5, seed 1, target-side reflow sampling, mid-time 0.6.
- `c51-reflow-target-mid60-c32h160-s14000-seed1`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 14000 steps, freq10, grad clip 0.5, seed 1, target-side reflow sampling, mid-time 0.6.
- `c48-layout-reflow-100-c32h160-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5.
- `c48-layout-reflow-100-c32h160-freq12-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq12, grad clip 0.5.
- `c48-layout-reflow-100-c40h192-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5.
- `c48-layout-reflow-100-c32h160-textw-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5, text-weighted.
- `c48-layout-reflow-100-textw-strong-s11000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 11000 steps, freq10, grad clip 0.5, text-weighted.
- `c48-layout-reflow-100-train1920-c24h128-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 10000 steps, freq10, grad clip 0.5.
- `c48-layout-reflow-100-train1920-c32h160-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5.
- `c48-layout-reflow-100-c32h160-lr007-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5.
- `c48-layout-reflow-100-c32h160-cosine-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5.
- `c48-layout-reflow-100-c32h160-edge16-s12000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 12000 steps, freq10, grad clip 0.5.
- `c47-layout-reflow-070-s9000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 0.7, flow scale 0.06, 9000 steps, freq10, grad clip 0.5.
- `c47-layout-reflow-085-s9000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 0.85, flow scale 0.08, 9000 steps, freq10, grad clip 0.5.
- `c47-layout-reflow-100-s9000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 9000 steps, freq10, grad clip 0.5.
- `c47-layout-reflow-100-freq12-s9000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 9000 steps, freq12, grad clip 0.5.
- `c47-layout-reflow-100-c24h128-s9000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 9000 steps, freq10, grad clip 0.5.
- `c47-layout-reflow-100-c32h160-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.1, 10000 steps, freq10, grad clip 0.5.
- `c47-layout-reflow-100-textw-s9000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 9000 steps, freq10, grad clip 0.5, text-weighted.
- `c47-layout-reflow-100-textw-c24h128-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 10000 steps, freq10, grad clip 0.5, text-weighted.
- `c47-layout-reflow-100-lr007-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 10000 steps, freq10, grad clip 0.5.
- `c47-layout-reflow-100-cosine-s10000`: done - Learned layout-reflow proof: the training target moves text/content blocks and resizes/repositions the illustration into a new page layout; output remains direct neural-canvas pixels. amount 1, flow scale 0.08, 10000 steps, freq10, grad clip 0.5.
- `c47-learned-translate-0035-s9000`: done - C46 learned independent-translation stress: train the renderer to generate local translations directly from x,y,t with scale disabled, motion 0.035, 9000 steps, freq10, grad clip 0.5.
- `c47-learned-translate-004-s9000`: done - C46 learned independent-translation stress: train the renderer to generate local translations directly from x,y,t with scale disabled, motion 0.04, 9000 steps, freq10, grad clip 0.5.
- `c47-learned-translate-0045-s9000`: done - C46 learned independent-translation stress: train the renderer to generate local translations directly from x,y,t with scale disabled, motion 0.045, 9000 steps, freq10, grad clip 0.5.
- `c47-learned-translate-004-c24h128-s9000`: done - C46 learned independent-translation stress: train the renderer to generate local translations directly from x,y,t with scale disabled, motion 0.04, 9000 steps, freq10, grad clip 0.5.
- `c47-learned-translate-004-textw-s9000`: done - C46 learned independent-translation stress: train the renderer to generate local translations directly from x,y,t with scale disabled, motion 0.04, 9000 steps, freq10, grad clip 0.5.
- `c47-sprite-translate-0035-s9000`: done - C47 learned sprite-translation stress: region content translates as a region in the training target, but output is still direct neural-canvas pixels; motion 0.035, 9000 steps, freq10, grad clip 0.5.
- `c47-sprite-translate-004-s9000`: done - C47 learned sprite-translation stress: region content translates as a region in the training target, but output is still direct neural-canvas pixels; motion 0.04, 9000 steps, freq10, grad clip 0.5.
- `c47-sprite-translate-004-c24h128-s9000`: done - C47 learned sprite-translation stress: region content translates as a region in the training target, but output is still direct neural-canvas pixels; motion 0.04, 9000 steps, freq10, grad clip 0.5.
- `c47-sprite-translate-004-textw-s9000`: done - C47 learned sprite-translation stress: region content translates as a region in the training target, but output is still direct neural-canvas pixels; motion 0.04, 9000 steps, freq10, grad clip 0.5, text-weighted.
- `c47-translate-pan040-clip05`: done - C46 independent-translation stress: smooth local fields translate page regions on separate timelines with scale disabled, pan 0.04, freq10, grad clip 0.5.
- `c46-translate-pan030-clip05`: done - C46 independent-translation stress: smooth local fields translate page regions on separate timelines with scale disabled, pan 0.03, freq10, grad clip 0.5.
- `c46-translate-pan045-clip05`: done - C46 independent-translation stress: smooth local fields translate page regions on separate timelines with scale disabled, pan 0.045, freq10, grad clip 0.5.
- `c46-translate-pan060-clip05`: done - C46 independent-translation stress: smooth local fields translate page regions on separate timelines with scale disabled, pan 0.06, freq10, grad clip 0.5.
- `c46-translate-pan075-clip05`: done - C46 independent-translation stress: smooth local fields translate page regions on separate timelines with scale disabled, pan 0.075, freq10, grad clip 0.5.
- `c46-translate-pan090-clip05`: done - C46 independent-translation stress: smooth local fields translate page regions on separate timelines with scale disabled, pan 0.09, freq10, grad clip 0.5.
- `c46-translate-pan060-freq12-clip05`: done - C46 independent-translation stress: smooth local fields translate page regions on separate timelines with scale disabled, pan 0.06, freq12, grad clip 0.5.
- `c46-translate-pan075-clip05-seed1`: done - C46 independent-translation stress: smooth local fields translate page regions on separate timelines with scale disabled, pan 0.075, freq10, grad clip 0.5, seed 1.
- `c46-learned-translate-004-s7000`: done - C46 learned independent-translation stress: train the renderer to generate local translations directly from x,y,t with scale disabled, motion 0.04, 7000 steps, freq10, grad clip 0.5.
- `c46-learned-translate-006-s7000`: done - C46 learned independent-translation stress: train the renderer to generate local translations directly from x,y,t with scale disabled, motion 0.06, 7000 steps, freq10, grad clip 0.5.
- `c46-learned-translate-008-s7000`: done - C46 learned independent-translation stress: train the renderer to generate local translations directly from x,y,t with scale disabled, motion 0.08, 7000 steps, freq10, grad clip 0.5.
- `c45-field-006-pan018-clip05`: done - C45 smooth independent-field stress: no hard region blanking; separate local affine fields blend continuously with strength 0.06, pan 0.018, freq10, grad clip 0.5.
- `c45-field-010-pan024-clip05`: done - C45 smooth independent-field stress: no hard region blanking; separate local affine fields blend continuously with strength 0.1, pan 0.024, freq10, grad clip 0.5.
- `c45-field-014-pan030-clip05`: done - C45 smooth independent-field stress: no hard region blanking; separate local affine fields blend continuously with strength 0.14, pan 0.03, freq10, grad clip 0.5.
- `c45-field-014-pan036-clip05`: done - C45 smooth independent-field stress: no hard region blanking; separate local affine fields blend continuously with strength 0.14, pan 0.036, freq10, grad clip 0.5.
- `c45-field-010-pan024-clip05-seed1`: done - C45 smooth independent-field stress: no hard region blanking; separate local affine fields blend continuously with strength 0.1, pan 0.024, freq10, grad clip 0.5, seed 1.
- `c45-learned-field-004-flow04`: done - C45 learned independent-field stress: train the renderer to generate multi-region motion directly from x,y,t with motion 0.04, freq10.
- `c45-learned-field-006-flow06`: done - C45 learned independent-field stress: train the renderer to generate multi-region motion directly from x,y,t with motion 0.06, freq10.
- `c45-learned-field-008-flow08`: done - C45 learned independent-field stress: train the renderer to generate multi-region motion directly from x,y,t with motion 0.08, freq10.
- `c45-learned-field-006-flow06-clip05`: done - C45 learned independent-field stress: train the renderer to generate multi-region motion directly from x,y,t with motion 0.06, freq10, grad clip 0.5.
- `c45-learned-field-006-flow06-freq12-clip05`: done - C45 learned independent-field stress: train the renderer to generate multi-region motion directly from x,y,t with motion 0.06, freq12, grad clip 0.5.
- `c44-regions-006-pan018-clip05`: done - C44 independent-region stress: coarse page regions move/scale at separate speeds with strength 0.06, pan 0.018, freq10, grad clip 0.5.
- `c44-regions-010-pan024-clip05`: done - C44 independent-region stress: coarse page regions move/scale at separate speeds with strength 0.1, pan 0.024, freq10, grad clip 0.5.
- `c44-regions-014-pan030-clip05`: done - C44 independent-region stress: coarse page regions move/scale at separate speeds with strength 0.14, pan 0.03, freq10, grad clip 0.5.
- `c44-regions-010-pan024-clip1`: done - C44 independent-region stress: coarse page regions move/scale at separate speeds with strength 0.1, pan 0.024, freq10, grad clip 1.
- `c44-regions-014-pan030-clip1`: done - C44 independent-region stress: coarse page regions move/scale at separate speeds with strength 0.14, pan 0.03, freq10, grad clip 1.
- `c44-regions-010-pan024-freq12-clip05`: done - C44 independent-region stress: coarse page regions move/scale at separate speeds with strength 0.1, pan 0.024, freq12, grad clip 0.5.
- `c44-regions-014-pan030-freq12-clip05`: done - C44 independent-region stress: coarse page regions move/scale at separate speeds with strength 0.14, pan 0.03, freq12, grad clip 0.5.
- `c44-regions-010-pan024-clip05-seed1`: done - C44 independent-region stress: coarse page regions move/scale at separate speeds with strength 0.1, pan 0.024, freq10, grad clip 0.5, seed 1.
- `c44-regions-010-pan024-clip05-seed2`: done - C44 independent-region stress: coarse page regions move/scale at separate speeds with strength 0.1, pan 0.024, freq10, grad clip 0.5, seed 2.
- `c44-regions-016-pan036-clip05`: done - C44 independent-region stress: coarse page regions move/scale at separate speeds with strength 0.16, pan 0.036, freq10, grad clip 0.5.
- `c43-frame-scale-014-edge09-freq10-train1536-clip05`: done - C43 clipping robustness: grad clip 0.5 at the hard 0.14 train1536/freq10 bracket.
- `c43-frame-scale-014-edge09-freq10-train1536-clip05-seed1`: done - C43 clipping robustness: grad clip 0.5 seed1 repeat at the hard 0.14 bracket.
- `c43-frame-scale-014-edge09-freq10-train1536-clip05-seed2`: done - C43 clipping robustness: grad clip 0.5 seed2 repeat at the hard 0.14 bracket.
- `c43-frame-scale-014-edge09-freq10-train1536-clip05-seed3`: done - C43 clipping robustness: grad clip 0.5 seed3 repeat at the hard 0.14 bracket.
- `c43-frame-scale-014-edge09-freq10-train1536-clip1-seed2`: done - C43 clipping robustness: grad clip 1.0 seed2 repeat after C42 seed1 success.
- `c43-frame-scale-014-edge09-freq10-train1536-clip1-seed3`: done - C43 clipping robustness: grad clip 1.0 seed3 repeat after C42 seed1 success.
- `c43-frame-scale-014-edge09-freq10-train1536-clip2`: done - C43 clipping robustness: looser grad clip 2.0 at the hard 0.14 bracket.
- `c43-frame-scale-014-edge09-freq10-train1536-clip2-seed1`: done - C43 clipping robustness: looser grad clip 2.0 seed1 repeat at the hard 0.14 bracket.
- `c43-frame-scale-014-edge09-freq10-train1536-lr007-clip1`: done - C43 clipping robustness: combine lr007 with grad clip 1.0 at the hard 0.14 bracket.
- `c43-frame-scale-014-edge09-freq10-train1536-lr007-clip1-seed1`: done - C43 clipping robustness: combine lr007 with grad clip 1.0 seed1 repeat.
- `c42-frame-scale-014-edge09-freq10-train1536-cosine`: done - C42 optimizer schedule: cosine LR decay at the fragile 0.14 train1536/freq10 bracket.
- `c42-frame-scale-014-edge09-freq10-train1536-cosine-seed1`: done - C42 optimizer schedule: cosine LR decay seed repeat at the 0.14 train1536/freq10 bracket.
- `c42-frame-scale-014-edge09-freq10-train1536-lr007-cosine`: done - C42 optimizer schedule: lower LR plus cosine decay at the 0.14 train1536/freq10 bracket.
- `c42-frame-scale-014-edge09-freq10-train1536-lr007-cosine-seed1`: done - C42 optimizer schedule: lower LR plus cosine decay seed repeat for the 0.14 bracket.
- `c42-frame-scale-014-edge09-freq10-train1536-clip1`: done - C42 optimizer schedule: gradient clipping at the 0.14 train1536/freq10 bracket.
- `c42-frame-scale-014-edge09-freq10-train1536-clip1-seed1`: done - C42 optimizer schedule: gradient clipping seed repeat at the 0.14 train1536/freq10 bracket.
- `c42-frame-scale-014-edge09-freq12-train1536-lr007-cosine`: done - C42 optimizer schedule: freq12 plus lower LR/cosine at the 0.14 bracket.
- `c42-frame-scale-014-edge09-freq12-train1536-lr007-cosine-seed1`: done - C42 optimizer schedule: freq12 plus lower LR/cosine seed repeat at the 0.14 bracket.
- `c42-frame-scale-0125-edge09-freq10-train1536-seed3-cosine`: done - C42 robustness: cosine LR on the weak 0.125 seed3 repeat.
- `c42-frame-scale-016-edge09-freq10-train1536-lr007-cosine`: done - C42 strong-resize optimizer schedule: lower LR/cosine on the 0.16 bracket.
- `c41-frame-scale-014-edge09-freq10-train1536-lr007`: done - C41 optimizer stability: lower learning rate at the 0.14 train1536/freq10 bracket.
- `c41-frame-scale-014-edge09-freq10-train1536-lr007-seed1`: done - C41 optimizer stability: lower learning rate seed repeat for the fragile 0.14 bracket.
- `c41-frame-scale-014-edge09-freq10-train1536-lr005`: done - C41 optimizer stability: conservative learning rate at the 0.14 train1536/freq10 bracket.
- `c41-frame-scale-014-edge09-freq10-train1536-lr005-seed1`: done - C41 optimizer stability: conservative learning rate seed repeat for the 0.14 bracket.
- `c41-frame-scale-014-edge09-freq12-train1536-seed1`: done - C41 frequency robustness: seed repeat for the 0.14 freq12 train1536 bracket.
- `c41-frame-scale-0125-edge09-freq10-train1536-seed2`: done - C41 robustness: seed 2 repeat for the new 0.125 train1536/freq10 winner family.
- `c41-frame-scale-0125-edge09-freq10-train1536-seed3`: done - C41 robustness: seed 3 repeat for the new 0.125 train1536/freq10 winner family.
- `c41-frame-scale-014-edge09-freq10-train1536-seed2`: done - C41 robustness: seed 2 repeat for the unstable 0.14 train1536/freq10 bracket.
- `c41-frame-scale-014-edge09-freq10-train1536-seed3`: done - C41 robustness: seed 3 repeat for the unstable 0.14 train1536/freq10 bracket.
- `c41-frame-scale-016-edge09-freq10-train1536-lr007`: done - C41 optimizer stability: lower learning rate on the strong 0.16 train1536/freq10 resize bracket.
- `c40-frame-scale-0125-edge09-freq10-train1536-seed1`: done - C40 denser-canvas robustness: seed repeat for the C39 0.125 train1536/freq10 winner.
- `c40-frame-scale-014-edge09-freq10-train1536-seed1`: done - C40 denser-canvas robustness: seed repeat for the C39 0.14 train1536/freq10 pass.
- `c40-frame-scale-0125-edge09-freq12-train1536`: done - C40 denser-canvas frequency ladder: test freq12 at the 0.125 train1536 bracket.
- `c40-frame-scale-014-edge09-freq12-train1536`: done - C40 denser-canvas frequency ladder: test freq12 at the 0.14 train1536 bracket.
- `c40-frame-scale-0125-edge075-freq10-train1536`: done - C40 denser-canvas edge bracket: lighter edge weighting around the C39 0.125 winner.
- `c40-frame-scale-014-edge075-freq10-train1536`: done - C40 denser-canvas edge bracket: lighter edge weighting at the 0.14 train1536 bracket.
- `c40-frame-scale-0125-edge09-freq10-train1920`: done - C40 denser-canvas resolution ladder: test 1920x1088 latent training at the 0.125 bracket.
- `c40-frame-scale-014-edge09-freq10-train1920`: done - C40 denser-canvas resolution ladder: test 1920x1088 latent training at the 0.14 bracket.
- `c40-frame-scale-0145-edge09-freq10-train1536`: done - C40 denser-canvas stress ladder: push train1536/freq10 past the 0.14 bracket.
- `c40-frame-scale-016-edge09-freq10-train1536`: done - C40 denser-canvas stress ladder: test whether train1536/freq10 can hold 0.16 resize.
- `c39-frame-scale-0125-edge09-freq10-cap24h128`: done - C39 resize representation: combine higher coordinate frequency with larger latent/channel capacity at 0.125.
- `c39-frame-scale-014-edge09-freq10-cap24h128`: done - C39 resize representation: combine higher frequency and capacity at the 0.14 stress bracket.
- `c39-frame-scale-0125-edge09-freq10-s6000`: done - C39 resize representation: more optimization steps on the current fast C38 freq10 winner.
- `c39-frame-scale-014-edge09-freq10-s6000`: done - C39 resize representation: more optimization steps for high-frequency coordinates at 0.14.
- `c39-frame-scale-014-edge09-aa175`: done - C39 resize sampling: 1.75x neural supersampling to search between C38 1.5x and 2x.
- `c39-frame-scale-014-edge09-aa175-freq10`: done - C39 resize sampling: combine 1.75x supersampling with the C38 high-frequency improvement.
- `c39-frame-scale-014-edge09-aa175-cap24h128`: done - C39 resize sampling: combine 1.75x supersampling with larger capacity at 0.14.
- `c39-frame-scale-0125-edge09-freq10-aa15`: done - C39 resize sampling: test whether light supersampling stacks with the fast 0.125 freq10 winner.
- `c39-frame-scale-0125-edge09-freq10-train1536`: done - C39 resize representation: train the high-frequency renderer on a denser 1536x864 latent canvas.
- `c39-frame-scale-014-edge09-freq10-train1536`: done - C39 resize representation: denser 1536x864 latent canvas at the 0.14 stress bracket.
- `c38-frame-scale-0125-edge09-aa15`: done - C38 resize model/render test: 1.5x neural supersampling at the C37 best 0.125 bracket.
- `c38-frame-scale-014-edge09-aa15`: done - C38 resize model/render test: 1.5x neural supersampling at 0.14 to test aliasing versus identity loss.
- `c38-frame-scale-014-edge09-aa2`: done - C38 resize model/render test: 2x neural supersampling to measure quality/latency tradeoff directly.
- `c38-frame-scale-016-edge075-aa15`: done - C38 resize model/render test: 1.5x neural supersampling on the strong 0.16 bracket.
- `c38-frame-scale-0125-edge09-cap24h128`: done - C38 resize model test: larger latent/channel capacity at the C37 best 0.125 bracket.
- `c38-frame-scale-014-edge09-cap24h128`: done - C38 resize model test: larger latent/channel capacity at the 0.14 resize bracket.
- `c38-frame-scale-0125-edge09-freq10`: done - C38 resize model test: higher coordinate frequency at the C37 best 0.125 bracket.
- `c38-frame-scale-014-edge09-freq10`: done - C38 resize model test: higher coordinate frequency at the 0.14 resize bracket.
- `c38-frame-scale-0125-edge09-seed1`: done - C38 resize robustness: seed repeat at the C37 best 0.125 bracket.
- `c38-frame-scale-014-edge09-seed1`: done - C38 resize robustness: seed repeat at the unstable 0.14 bracket.
- `c37-frame-scale-012-edge09-s6000`: done - C37 resize refinement: repeat the best C35 0.12/edge09 bracket with more optimization steps.
- `c37-frame-scale-0125-edge09`: done - C37 resize refinement: locate the cliff just above the C35 0.12 pass.
- `c37-frame-scale-013-edge09`: done - C37 resize refinement: mid-point between C35 0.12 and C36 0.14.
- `c37-frame-scale-0135-edge09`: done - C37 resize refinement: upper mid-point before the C36 0.14 quality drop.
- `c37-frame-scale-014-edge09-s6000`: done - C37 resize refinement: test whether more steps rescue the C36 0.14/edge09 setting.
- `c37-frame-scale-014-edge075-s6000`: done - C37 resize refinement: more steps on the lighter-edge 0.14 bracket.
- `c37-frame-scale-016-edge075-s6000`: done - C37 resize refinement: check whether the 0.16 strong-resize pass improves with more steps.
- `c37-frame-scale-014-edge09-pan000`: done - C37 resize isolation: remove pan at 0.14/edge09 to separate scaling from repositioning.
- `c37-frame-scale-014-edge09-pan018`: done - C37 resize isolation: reduced pan at 0.14/edge09 to quantify scale-plus-reposition cost.
- `c37-frame-scale-014-edge075-pan000`: done - C37 resize isolation: no-pan version of the lighter-edge 0.14 bracket.
- `c36-frame-scale-014-edge075`: done - C36 resize cliff: frame-scale 0.14 with lighter edge weighting, no OCR boxes.
- `c36-frame-scale-014-edge09`: done - C36 resize cliff: frame-scale 0.14 at the better C35 edge09 setting.
- `c36-frame-scale-014-edge1`: done - C36 resize cliff: frame-scale 0.14 at edge1 to locate the degradation slope.
- `c36-frame-scale-016-edge075`: done - C36 strong resize: test whether lighter edge weighting rescues the C35 0.16 near-miss.
- `c36-frame-scale-016-edge09`: done - C36 strong resize: edge09 version of the 0.16 resize stress.
- `c36-frame-scale-016-edge09-s6000`: done - C36 strong resize: more optimization steps at the best likely 0.16 edge bracket.
- `c36-frame-scale-018-edge075`: done - C36 over-stress: frame-scale 0.18 with lighter edge weighting to find the hard failure point.
- `c36-frame-scale-018-edge05`: done - C36 over-stress: frame-scale 0.18 with very light edge weighting.
- `c36-learned-frame-scale-006-flow04`: done - C36 learned resize: train the neural renderer itself on mild frame-scale motion, no layout transform.
- `c36-learned-frame-scale-008-flow05`: done - C36 learned resize: stronger learned frame-scale motion with higher flow budget.
- `c35-viewport-014-edge1-z006`: done - C35 aggressive viewport stress: no-OCR winner under zoom-pulse viewport movement.
- `c35-viewport-014-edge1-z010`: done - C35 aggressive viewport stress: larger zoom/pan, still no OCR boxes or masks.
- `c35-viewport-014-edge1-z014`: done - C35 aggressive viewport stress: high zoom/pan to find query-stability failure modes.
- `c35-frame-scale-008-edge1`: done - C35 resize stress: global frame-scale layout transform with no OCR boxes.
- `c35-frame-scale-012-edge1`: done - C35 resize stress: stronger global frame-scale layout transform with no OCR boxes.
- `c35-frame-scale-016-edge1`: done - C35 resize stress: strong global frame-scale layout transform to expose text repainting.
- `c35-responsive-014-edge1-z006`: done - C35 combined stress: responsive-squeeze plus viewport zoom, no OCR boxes.
- `c35-responsive-016-edge1-z008`: done - C35 combined stress: stronger responsive-squeeze plus viewport zoom.
- `c35-frame-scale-012-edge09`: done - C35 resize bracket: frame-scale with slightly lighter edge weighting.
- `c35-control-wiggle-014-edge1`: done - C35 control only: one local-motion repeat so aggressive tests can be compared to the known winner.
- `c34-general-flow-014-edge1-seed0`: done - C34 robustness: repeat the C33 no-OCR winner with explicit deterministic seed 0.
- `c34-general-flow-014-edge1-seed1`: done - C34 robustness: repeat the C33 no-OCR winner with seed 1.
- `c34-general-flow-014-edge1-seed2`: done - C34 robustness: repeat the C33 no-OCR winner with seed 2.
- `c34-general-flow-014-edge1-seed3`: done - C34 robustness: repeat the C33 no-OCR winner with seed 3.
- `c34-general-flow-0141-edge1-seed0`: done - C34 cliff test: nudge pure no-OCR flow just above the C33 winner.
- `c34-general-flow-01415-edge1-seed0`: done - C34 cliff test: bracket the narrow gap before the C33 0.01425 near-miss.
- `c34-general-flow-0142-edge1-seed0`: done - C34 cliff test: test whether 0.0142 remains viable before the 0.01425 readability drop.
- `c34-general-flow-014-edge09-seed0`: done - C34 edge bracket: test slightly lighter edge weighting at the C33 winner flow.
- `c34-general-responsive-014-edge1-seed0`: done - C34 responsive stress: apply the current winner flow to the no-OCR responsive-squeeze case.
- `c34-general-viewport-014-edge1-seed0`: done - C34 viewport stress: keep learned jiggle but add viewport zoom/pan to test query stability.
- `c33-general-flow-0135-edge075`: done - Scale-up pure neural canvas: bracket the C32 no-OCR winner with slightly lighter edge weighting.
- `c33-general-flow-0135-edge15`: done - Scale-up pure neural canvas: bracket the C32 no-OCR winner with slightly stronger edge weighting.
- `c33-general-flow-014-edge1`: done - Scale-up pure neural canvas: push no-OCR flow beyond the C32 winner at the best edge setting.
- `c33-general-flow-01425-edge1`: done - Scale-up pure neural canvas: find the no-OCR readability cliff above flow 0.014.
- `c33-general-flow-0135-edge1-s6000`: done - Scale-up pure neural canvas: more optimization steps at the C32 no-OCR winner.
- `c33-general-flow-0135-edge1-cap24h128`: done - Scale-up pure neural canvas: larger latent canvas/channel capacity on the current no-OCR winner.
- `c33-general-flow-014-edge05`: done - Scale-up pure neural canvas: push motion with lighter edge weighting after C32 edge05 improved quality.
- `c33-text-flow-01375-box8`: done - Scale-up bridge: split the gap between C31 box8 success and C32/C31 higher-flow failures.
- `c33-text-flow-0135-box6`: done - Scale-up bridge: reduce OCR-box loss at the bridge winner flow to see if text boxes can be less dominant.
- `c33-general-responsive-012-edge1`: done - Scale-up pure neural canvas: no-OCR responsive-squeeze stress at a lower amplitude.
- `c32-general-flow-0125-edge05`: done - General neural-canvas refinement: lighter dense edge weighting around the C31 no-OCR winner.
- `c32-general-flow-0135-edge1`: done - General neural-canvas refinement: push the best no-OCR edge setting to the bridge winner's motion level.
- `c32-text-flow-0145-box8`: done - Text-aware learned-flow bridge: push beyond the C31 0.0135 bridge while avoiding render-time masks.
- `c32-text-flow-0135-box12`: done - Text-aware learned-flow bridge: stronger OCR-box training signal at the current bridge winner flow.
- `c31-general-flow-0125-edge1`: done - General neural-canvas direction: no OCR boxes, baseline edge/glyph weighting at the C30 sweet-spot flow.
- `c31-general-flow-0125-edge4`: done - General neural-canvas direction: no OCR boxes, stronger dense edge/glyph weighting at the C30 sweet-spot flow.
- `c31-general-flow-010-edge4`: done - General neural-canvas direction: no OCR boxes, lower-flow recovery point with dense edge/glyph weighting.
- `c31-general-flow-0125-edge8`: done - General neural-canvas direction: no OCR boxes, very strong dense edge/glyph weighting to test over-sharpening versus readability.
- `c31-text-flow-0135-box8`: done - Text-aware learned-flow bridge: OCR boxes affect training loss only, no output masks or layout anchors.
- `c31-text-flow-014-box8`: done - Text-aware learned-flow bridge: push just past the C30 boundary without render-time masks.
- `c31-text-flow-0125-box12`: done - Text-aware learned-flow bridge: stronger OCR-box sampling/loss at the C30 sweet spot, still no output masks.
- `c31-alpha-layout-r0025-s004`: done - Shape-aware bridge: glyph alpha selection under gentle layout motion, testing a non-rectangle rescue path.
- `c30-gentle-flow-0125`: done - Pleasant-motion boundary between the C29 0.010 pass and 0.020 quality drop.
- `c30-gentle-flow-015`: done - Pleasant-motion boundary probe near the likely readability cliff.
- `c30-product-layout-r0025-s008`: done - Pleasant layout-motion with best line ratio and moderate layout strength.
- `c30-line-batched-r0025-strong`: done - Batched control for the current best strong-stress line ratio.
- `c29-gentle-flow-010`: done - Pleasant-motion ladder: double the C2.1 flow while trying to preserve text.
- `c29-gentle-flow-020`: done - Pleasant-motion ladder: stronger learned motion, still below resize/reflow stress.
- `c29-product-layout-r0025`: done - Pleasant layout-motion baseline using the best strong-stress line ratio at low layout strength.
- `c28-word-batched-r010`: done - Human-positive word anchors with batched patch rendering to recover latency.
- `c28-word-batched-moderate-r010`: done - Human-positive word anchors batched under moderate stress.
- `c28-line-batched-r005`: done - Batched line-anchor control should match C2.6 quality while confirming batching does not change pixels.
- `c27-line-rect-r0025`: done - Line anchor ratio sweep below current best 0.05.
- `c27-line-rect-r0075`: done - Line anchor ratio sweep above current best 0.05 but below previous 0.10.
- `c27-line-rect-r005-pad2`: done - Current best ratio with tighter support boxes to reduce unrelated illustration capture.
- `c27-line-rect-r005-pad6`: done - Current best ratio with wider support boxes to check whether extra local context improves OCR.
- `c27-responsive-018`: done - Responsive-squeeze boundary between strong 0.16 pass and xstrong 0.22 fail.
- `c26-word-rect-r010`: done - Word-level support rectangles: preserve text background while avoiding large line boxes that can catch illustration pixels.
- `c26-word-rect-moderate-r010`: done - Moderate stress word-level support rectangle control.
- `c26-line-rect-r005`: done - Line anchor scale-ratio refinement below the current best 0.10.
- `c26-responsive-xstrong`: done - Very strong responsive-squeeze boundary after the strong run still passed.
- `c25-alpha-r000`: done - Relaunch text-alpha strong stress ratio 0.00 after run-id collision fix.
- `c25-alpha-r025`: done - Relaunch text-alpha strong stress ratio 0.25 after run-id collision fix.
- `c25-alpha-moderate-r010`: done - Check whether text-alpha only fails under strong stress or also moderate stress.
- `c25-rectangle-r010`: done - Rectangle anchor ratio 0.10 control to separate alpha-mask failure from scale-ratio behavior.
- `c25-responsive-strong`: done - Stronger responsive-squeeze boundary after the first responsive-squeeze pass.
